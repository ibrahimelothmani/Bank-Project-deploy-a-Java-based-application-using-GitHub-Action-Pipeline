apiVersion: v1
kind: Service
metadata:
  name: bank-project
  labels:
    app: bank-project
spec:
  type: NodePort
  selector:
    app: bank-project
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
